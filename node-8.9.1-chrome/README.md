# Node 8.9.0 LTS w/Chrome Headless

This image is used to bake in Debian dependencies for using Chrome Headless to speed up builds that use Karma + Chrome Headless to do tests. It is based on the specified Node.js version (typically latest LTS).